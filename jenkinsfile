pipeline {
    agent none
    stages{
        stage('Execute Playbook'){
            steps {
                ansiblePlaybook becomeUser: 'ubuntu', installation: 'ansiblexec', inventory: 'ansible.inv', playbook: 'test.yml', sudoUser: 'ubuntu'
            }
        }
    }
}
