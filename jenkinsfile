pipeline {
    agent any
    stages{
        stage('Execute Playbook'){
            steps {
                ansiblePlaybook installation: 'ansiblexec', inventory: 'ansible.inv', playbook: 'test.yml', sudoUser: 'ubuntu'
            }
        }
    }
}
